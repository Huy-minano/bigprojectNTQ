import classNames from "classnames/bind"
import styles from './ChildMenutrailer.module.scss'
const cx = classNames.bind(styles)
const ChildMenutrailer = () => {
  return (
    <div className={cx('wrapper')}>ChildMenutrailer</div>
  )
}

export default ChildMenutrailer